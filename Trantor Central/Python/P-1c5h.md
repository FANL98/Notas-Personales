```python title:secuencia.py
palabra = 'Python'
for letra in palabra:
    print(letra) #P, y, t, h, o, n en líneas separadas
```

Los "strings" son secuencias, por lo que podemos iterar en ellos.

🏷️:  #ciclo #iterar #orden #secuencia #string #índice 

🔙 [[P-1c5]]